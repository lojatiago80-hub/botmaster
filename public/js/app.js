const $=s=>document.querySelector(s),content=$('#content');let bots=[];async function api(url,opt={}){opt.headers={...(opt.headers||{}),'content-type':'application/json'};const r=await fetch(url,opt);let j={};try{j=await r.json()}catch{}if(r.status===401){showLogin();throw Error('Não autenticado')}return j}function showLogin(){$('#login').classList.remove('hidden');$('#app').classList.add('hidden')}function showApp(){$('#login').classList.add('hidden');$('#app').classList.remove('hidden')}function esc(s=''){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function lines(v){return String(v||'').split(/\r?\n/).map(x=>x.trim()).filter(Boolean)}
async function loadBots(){const r=await api('/api/bots');bots=r.bots||[];return bots}function live(b){return b.live||{};}function badge(b){const l=live(b);if(l.online)return '<span class="badge online">SL ONLINE</span>';if(l.reachable===false)return '<span class="badge offline">AGENT OFFLINE</span>';return `<span class="badge pending">${esc(l.phase||'OFFLINE')}</span>`}
async function dash(){await loadBots();$('#title').textContent='Dashboard';$('#sub').textContent='Todos os bots, mesmo estando em contas Render diferentes.';const on=bots.filter(b=>live(b).online).length,reach=bots.filter(b=>live(b).reachable!==false).length;content.innerHTML=`<div class="grid"><div class="card"><div class="muted">Bots cadastrados</div><h1>${bots.length}</h1></div><div class="card"><div class="muted">Second Life online</div><h1>${on}</h1></div><div class="card"><div class="muted">Agents acessíveis</div><h1>${reach}</h1></div></div><div class="grid">${bots.map(card).join('')}</div>`}
function card(b){return `<div class="card botcard"><h3>${esc(b.name)}</h3>${badge(b)}<p class="muted">${esc(b.firstName)} ${esc(b.lastName)}<br>${esc(b.agentUrl)}</p><div class="actions"><button class="ok" onclick="act(${b.id},'start')">Ligar</button><button class="danger" onclick="act(${b.id},'stop')">Desligar</button><button class="secondary" onclick="act(${b.id},'restart')">Reiniciar bot</button><button onclick="act(${b.id},'home')">Home</button></div></div>`}
async function botsPage(){await loadBots();$('#title').textContent='Bots';$('#sub').textContent='Cadastre e edite bots aqui.';content.innerHTML=`<button onclick="openBotForm()">+ Cadastrar Bot</button><div style="height:12px"></div>${bots.map(b=>`<div class="card"><b>#${b.id} ${esc(b.name)}</b> ${badge(b)}<div class="actions" style="margin-top:10px"><button onclick="openBotForm(${b.id})">Editar</button><button class="danger" onclick="removeBot(${b.id})">Excluir</button><button class="secondary" onclick="renderAct(${b.id},'restart')">Reiniciar Render</button><button class="warn" onclick="renderAct(${b.id},'deploy')">Deploy</button><button class="secondary" onclick="renderAct(${b.id},'suspend')">Suspender Render</button><button class="ok" onclick="renderAct(${b.id},'resume')">Retomar Render</button></div></div>`).join('')}`}
async function openBotForm(id){let b=id?bots.find(x=>x.id===id):null;if(id&&!b){const r=await api('/api/bots/'+id);b=r.bot}b=b||{name:'',firstName:'',lastName:'Resident',password:'',start:'last',agentUrl:'',agentSecret:'',renderServiceId:'',renderApiKey:'',group:{},home:{},features:{autoAcceptGroups:true,allowTeleportLinks:true,reconnectMs:15000},security:{allowedAdminNames:[],allowedAdminUUIDs:[]}};$('#title').textContent=id?'Editar Bot':'Cadastrar Bot';content.innerHTML=`<div class="card"><div class="row"><div><label>Nome no painel</label><input id="name" value="${esc(b.name)}"><label>First name SL</label><input id="first" value="${esc(b.firstName)}"><label>Last name SL</label><input id="last" value="${esc(b.lastName)}"><label>Senha SL</label><input id="slpass" type="password" placeholder="${id?'deixe em branco para manter':'senha'}"><label>Start</label><input id="start" value="${esc(b.start||'last')}"></div><div><label>Agent URL</label><input id="agentUrl" value="${esc(b.agentUrl)}" placeholder="https://meu-agent.onrender.com"><label>Agent Secret</label><input id="agentSecret" type="password" placeholder="${id?'deixe em branco para manter':'uma chave longa'}"><label>Render Service ID (opcional)</label><input id="renderId" value="${esc(b.renderServiceId||'')}"><label>Render API Key (opcional)</label><input id="renderKey" type="password" placeholder="${id?'deixe em branco para manter':'rnd_...'}"></div></div><h3>Grupo / Home</h3><div class="row"><div><input id="groupId" placeholder="Group UUID" value="${esc(b.group?.defaultGroupID||'')}"><input id="roleId" placeholder="Role UUID" value="${esc(b.group?.defaultRoleID||'')}"><input id="roleName" placeholder="Role name" value="${esc(b.group?.defaultRoleName||'')}"></div><div><input id="homeUrl" placeholder="URL Home" value="${esc(b.home?.url||'')}"><input id="homeObj" placeholder="Objeto UUID para sentar" value="${esc(b.home?.objectUUID||'')}"><label><input style="width:auto" type="checkbox" id="autoSit" ${b.home?.autoSit?'checked':''}> Auto sit</label></div></div><h3>Segurança</h3><div class="row"><textarea id="admins" placeholder="Nomes admin, um por linha">${esc((b.security?.allowedAdminNames||[]).join('\n'))}</textarea><textarea id="adminIds" placeholder="UUIDs admin, um por linha">${esc((b.security?.allowedAdminUUIDs||[]).join('\n'))}</textarea></div><label><input style="width:auto" type="checkbox" id="onlyAdmins" ${b.features?.onlyAllowedAdmins?'checked':''}> Somente admins podem comandar</label> <label><input style="width:auto" type="checkbox" id="autoGroups" ${b.features?.autoAcceptGroups!==false?'checked':''}> Aceitar grupos</label> <label><input style="width:auto" type="checkbox" id="tp" ${b.features?.allowTeleportLinks!==false?'checked':''}> Aceitar links TP</label><br><button onclick="save(${id||0})">Salvar</button><button class="secondary" onclick="botsPage()">Cancelar</button></div>`}
async function save(id){const body={name:$('#name').value,bot:{firstName:$('#first').value,lastName:$('#last').value,password:$('#slpass').value,start:$('#start').value},agentUrl:$('#agentUrl').value,agentSecret:$('#agentSecret').value,renderServiceId:$('#renderId').value,renderApiKey:$('#renderKey').value,group:{defaultGroupID:$('#groupId').value,defaultRoleID:$('#roleId').value,defaultRoleName:$('#roleName').value},home:{url:$('#homeUrl').value,objectUUID:$('#homeObj').value,autoSit:$('#autoSit').checked},features:{onlyAllowedAdmins:$('#onlyAdmins').checked,autoAcceptGroups:$('#autoGroups').checked,allowTeleportLinks:$('#tp').checked,reconnectMs:15000},security:{allowedAdminNames:lines($('#admins').value),allowedAdminUUIDs:lines($('#adminIds').value)}};const r=await api(id?'/api/bots/'+id:'/api/bots',{method:id?'PUT':'POST',body:JSON.stringify(body)});if(!r.ok)return alert(r.error||'Erro');alert('Salvo.');botsPage()}
async function act(id,a){const r=await api(`/api/bots/${id}/${a}`,{method:'POST'});alert(r.message||r.error||'OK');dash()}async function renderAct(id,a){const r=await api(`/api/bots/${id}/render/${a}`,{method:'POST'});alert(r.error||'Comando enviado ao Render.');botsPage()}async function removeBot(id){if(!confirm('Excluir bot?'))return;await api('/api/bots/'+id,{method:'DELETE'});botsPage()}
async function logsPage(){await loadBots();$('#title').textContent='Logs';content.innerHTML=`<div class="card"><select id="logbot">${bots.map(b=>`<option value="${b.id}">#${b.id} ${esc(b.name)}</option>`).join('')}</select><button onclick="getLogs()">Carregar</button><pre id="box"></pre></div>`}async function getLogs(){const r=await api('/api/bots/'+$('#logbot').value+'/logs');$('#box').textContent=(r.logs||[]).join('\n')}
function configPage(){$('#title').textContent='Configuração';content.innerHTML=`<div class="card"><h3>Master Render</h3><pre>DATABASE_URL=postgresql://...neon...\nADMIN_PASSWORD=sua_senha\nSESSION_SECRET=chave_longa\nDATA_ENCRYPTION_KEY=outra_chave_longa</pre><h3>Cada Agent Render</h3><pre>MASTER_URL=https://seu-master.onrender.com\nBOT_ID=1\nAGENT_SECRET=a-mesma-chave-cadastrada-no-painel</pre><p class="muted">Cada Agent pode estar em uma conta Render diferente.</p></div>`}
async function route(p){document.querySelectorAll('aside button[data-page]').forEach(x=>x.style.opacity=x.dataset.page===p?'1':'.65');if(p==='dash')dash();if(p==='bots')botsPage();if(p==='logs')logsPage();if(p==='config')configPage()}
$('#enter').onclick=async()=>{const r=await api('/api/login',{method:'POST',body:JSON.stringify({password:$('#pwd').value})});if(r.ok){showApp();route('dash')}else $('#loginMsg').textContent=r.error||'Erro'};$('#logout').onclick=async()=>{await api('/api/logout',{method:'POST'});showLogin()};$('#refresh').onclick=()=>route('dash');document.querySelectorAll('aside button[data-page]').forEach(b=>b.onclick=()=>route(b.dataset.page));api('/api/me').then(()=>{showApp();route('dash')}).catch(()=>showLogin());
